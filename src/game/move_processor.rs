pub fn process_player_move(
    player_move: &String,
    game_board: &Vec<Vec<char>>,
    tile: &Vec<String>,
) -> Option<Vec<Vec<char>>> {
    let mut output_board: Vec<Vec<char>> = game_board.to_vec();
    // println!("{player_move}");

    Some(output_board)
}
